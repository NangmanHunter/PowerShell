- 모듈에서 Export-ModuleMember를 안 쓰면 어떻게 되느냐?
- ➡ 함수가 외부로 노출되지 않아서, Import-Module 뒤에 함수가 안 보인다.
- ➡ 즉, 모듈을 import해도 함수 호출이 불가능해진다.
- PowerShell 모듈(.psm1)에는 "모듈 스코프"라는 것이 있다.
- psm1 안에서 dot-source로 불러오든 직접 정의하든 함수는 기본적으로 모두 모듈 내부 스코프에 들어감.
- 그런데 외부에서 모듈을 호출할 때는 Export된 것만 보여줘.